#include<iostream>
#include<bits/stdc++.h>
# define mod 1000000007
#define  int long long
using namespace std;
signed main(){
        int n; cin>>n;
        int arr[n];
        for(int i=0;i<n;i++){
          cin>>arr[i];
        }
        int m=arr[0];
        for(int i=0;i<n;i++){
             m=max(m,arr[i]);
        }
        int ans=0;
        for(int i=0;i<n;i++){
          ans=ans+(m-arr[i]);
        }
        cout<<ans<<" ";
}